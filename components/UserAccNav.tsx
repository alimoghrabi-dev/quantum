interface UserAccNavProps {
  email: string | undefined;
  imageUrl: string;
  name: string;
}

const UserAccNav = ({ email, imageUrl, name }: UserAccNavProps) => {
  return <div></div>;
};

export default UserAccNav;
